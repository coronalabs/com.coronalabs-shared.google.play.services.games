Sources for the plugin `shared.google.play.services.games`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.google.play.services.games" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
